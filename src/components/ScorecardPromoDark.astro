---
type PromoContext = 'services' | 'resources' | 'contact';
interface Props { context: PromoContext; image: string; }
const { context, image } = Astro.props;
const text = {
 services:{en:['FREE ARTIST DIAGNOSTIC','NOT SURE WHICH SERVICE YOUR PROJECT NEEDS?','Answer 20 focused questions and receive a clearer view of your identity, sound, visual